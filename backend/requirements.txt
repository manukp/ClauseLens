# ClauseLens backend — Phase 1 (scaffold + AWS spine) + Phase 2 (Stage-1 ingest).
fastapi==0.115.6
uvicorn[standard]==0.34.0
boto3==1.35.90
pydantic==2.10.4
python-multipart==0.0.20        # P2: file uploads (POST /files)

# Phase 2 — Stage-1 ingest + LangGraph spine.
langgraph==0.2.61               # orchestration spine only (D1)
PyMuPDF==1.25.1                 # parsing, page+bbox (D4) — import name: fitz
faiss-cpu==1.9.0.post1          # local vector index (D5)
numpy==1.26.4                   # vector math for FAISS

# Tests
pytest==8.3.4
httpx==0.28.1
