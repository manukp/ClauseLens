"""Stage-1 ingest (Phase 2): parse -> chunk -> embed -> extract -> summarize.

PyMuPDF only (D4); models via the boto3 Converse wrappers (D2); every artifact
cited deterministically from chunk provenance (D9 / D18).
"""
