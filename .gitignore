# Local AWS config (embeds account id / bucket); never commit.
config.env

# Project planning + standing context (kept local).
.claude/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
backend/.venv/

# Local job store + uploads (created lazily at runtime).
data/

# Node / frontend build
frontend/node_modules/
frontend/dist/
node_modules/

# OS / editor
.DS_Store
.vscode/
.idea/
